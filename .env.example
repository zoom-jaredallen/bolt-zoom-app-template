# Zoom App Configuration
# Get these values from: https://marketplace.zoom.us/develop/create

# Your Zoom App Client ID (public, can be used client-side)
VITE_ZOOM_CLIENT_ID=your_zoom_client_id_here

# OAuth redirect URL (use bolt.diy's proxy for development)
VITE_OAUTH_REDIRECT_URL=https://zoomvibes.j4red4llen.com/api/oauth/proxy/callback

# Your app's public URL (where your app is hosted)
VITE_PUBLIC_URL=https://your-app-domain.com

# Server-side secrets (NEVER expose these in client code!)
# These should be set in your deployment environment, not committed to git
# ZOOM_CLIENT_SECRET=your_zoom_client_secret_here
